<Page
    x:Class="MikeRobbins.SUGCON.Beacons.Kiosk.Pages.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer Padding="{x:Bind Frame.Padding, Mode=OneWay}">
            <StackPanel>
                <TextBlock Text="Welcome" Style="{StaticResource TitleTextBlockStyle}" DataContext="{x:Bind Frame}" Visibility="{Binding PageTitleVisibility}" />

                <TextBlock Text="Please enter your email address to retrieve your zoo visit details."  Style="{StaticResource ParagraphTextBlockStyle}"></TextBlock>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Name="Name" Text="Email Address:" Grid.Column="0" Grid.Row="0" VerticalAlignment="Center"  />
                    <TextBox Name="EmailAddress" Grid.Column="1" Grid.Row="0"></TextBox>
                    <Button Name="ContactLookUp" Content="Load Account" Grid.Column="2" Grid.Row="0" Click="ContactLookUp_OnClick"></Button>
                </Grid>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
